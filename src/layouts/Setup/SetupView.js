import { StudentPicker, GroupPicker } from '~/containers/Pickers';

function SetupView(props) {
    return (
        <div>
            <StudentPicker />
            <GroupPicker />
        </div>
    );
}

export default SetupView;